[workspace]
# Skip PR creation and release directly
pr_draft = false
pr_labels = ["release"]
git_release_enable = true
git_tag_enable = true
changelog_update = true

[[package]]
name = "tempo-cli"
# Conventional commit based version bumping
semver_check = false
# Always bump patch version for any commit to main
release = true
# Skip PR creation - release directly
pr = false
